@using WebApp_Exercise.Presentations.ViewModels
@model DepartmentsEnterViewModel
@{
    ViewData["Title"] = "部門登録（完了）";
}
<h2 class="text-center mt-4">@ViewData["Title"]</h2>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-sm-8 text-center">

            @* 登録完了メッセージ *@
            <div class="alert alert-success" role="alert">
                部門の登録が完了しました。
            </div>

            <table class="table table-hover">
                <tbody>
                    @* 登録した部門情報を表示 *@
                    <tr>
                        <td class="fw-bold">
                            @Html.DisplayNameFor(m => m.DeptName)
                        </td>
                        <td>
                            @Html.DisplayFor(m => m.DeptName)
                        </td>
                    </tr>
                    <tr>
                        <td class="fw-bold">
                            @Html.DisplayNameFor(m => m.CreatedEmpNo)
                        </td>
                        <td>
                            @Html.DisplayFor(m => m.CreatedEmpNo)
                        </td>
                    </tr>

                
                </tbody>
            </table>

            @* 続けて登録 *@
            <a asp-action="DepartmentsEnter"
               asp-controller="Departments"
               class="btn btn-primary mt-3">続けて登録する</a>

            @* メニューに戻る *@
            <a asp-action="Index"
               asp-controller="Home"
               class="btn btn-secondary mt-3 ms-2">メニューに戻る</a>
        </div>
    </div>
</div>