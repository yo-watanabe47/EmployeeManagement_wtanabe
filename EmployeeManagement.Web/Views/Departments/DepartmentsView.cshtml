@using WebApp_Exercise.Presentations.ViewModels
@model IEnumerable<DepartmentsViewModel>
<table class="table">
    <thead>
        <tr>
            <th>部門ID</th>
            <th>部門名</th>
            <th>作成日時</th>
            <th>作成者ID</th>
            <th>更新日時</th>
            <th>更新者ID</th>
        </tr>
    </thead>
    <tbody>
          @foreach (var dept in Model)
            {
                <tr>
                    <td>@dept.Id</td>
                    <td>@dept.DeptName</td>
                    <td>@dept.CreatedAt?</td>
                    <td>@dept.CreatedEmpNo</td>
                    <td>@dept.UpdatedAt?</td>
                    <td>@dept.UpdatedEmpNo</td>
                </tr>
            }
    </tbody>
</table>